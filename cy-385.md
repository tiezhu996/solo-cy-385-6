请根据以下要求生成项目代码和 README。

## 项目主要需求

### 项目概述
**项目类型：全栈Web应用**
搭建一个宝宝成长记录与辅食管理平台，帮助新手父母记录宝宝的生长发育数据、疫苗接种、辅食食谱，生成成长报告。

### 功能需求

1. **宝宝档案管理**：创建宝宝档案，记录出生日期、身高、体重、血型等基础信息，支持多个宝宝档案切换。
2. **生长发育记录**：定期记录身高、体重数据，自动对比 WHO 儿童生长标准曲线，以图表形式展示发育趋势。
3. **疫苗接种管理**：根据国家免疫规划内置疫苗时间表，标记已接种/未接种，临近接种日期自动提醒。
4. **辅食食谱库**：按月龄（6-36个月）推荐合适辅食，每道食谱含食材清单、制作步骤、营养成分，支持按过敏原筛选。
5. **喂养日记**：记录每日喂养内容（母乳/配方奶/辅食）、喂养时间、宝宝反应（喜欢/一般/拒绝），追踪过敏反应。
6. **里程碑记录**：标记宝宝重要成长里程碑（第一次翻身、第一颗牙、第一步等），上传照片，生成时间线。
7. **数据统计**：月度喂养频次统计、辅食种类多样性评分、生长曲线百分位展示。

### 技术要求

- 前端：Vue 3 + TypeScript + Vant（移动端优先），构建工具 Vite
- 后端：Spring Boot + Java 17，ORM 使用 MyBatis-Plus
- 数据库：MySQL 8.0
- 认证：JWT
- 图表：ECharts
- 代码规范：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、疫苗类型枚举、月龄阶段）统一维护在后端 `backend/src/main/java/*/constants/` 目录下；日志统一使用 SLF4J + Logback，配置在 `backend/src/main/resources/logback-spring.xml`；异常信息统一定义在 `backend/src/main/java/*/exception/`，通过 @ControllerAdvice 全局异常处理器统一捕获并返回标准格式响应。

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面，组件，路由，状态管理，API 请求，类型定义，样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

后端目录结构建议：
```
backend/
├── src/main/java/com/babytracker/
│   ├── controller/         # 控制器（按模块拆分）
│   │   ├── UserController.java
│   │   ├── BabyController.java
│   │   ├── GrowthController.java
│   │   ├── VaccineController.java
│   │   └── FoodController.java
│   ├── service/            # 业务逻辑
│   ├── mapper/             # MyBatis Mapper
│   ├── entity/             # 数据库实体
│   ├── dto/                # 数据传输对象
│   ├── config/             # 配置类
│   ├── constants/          # 常量、错误码、枚举
│   ├── exception/          # 统一异常定义与处理器
│   └── utils/              # 工具类
├── src/main/resources/
│   ├── application.yml
│   └── logback-spring.xml
├── database/
│   └── init.sql
└── pom.xml
```

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=babytracker`
- `frontend/Dockerfile`：多阶段构建
- `backend/Dockerfile`：Java 后端构建镜像
- `frontend/nginx.conf`
- `.env.example`

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: babytracker`
- 容器名带 `babytracker-` 前缀
- 数据库数据使用命名卷持久化
- 前端 Nginx 反向代理 `/api` → `backend:8080`
- 数据库配置 `healthcheck`，后端 `depends_on` 数据库健康
- 端口映射：
  - 前端：18405:80
  - 后端：19405:8080

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目名称和一句话介绍、项目主要功能、快速启动方式（Docker Compose）、本地开发方式、访问地址（http://localhost:18405）、技术栈（表格展示）、项目目录结构（代码块展示）、环境变量说明、Docker 部署说明、License。

要求：Docker 部署命令放在最前面；告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   node_modules/
   dist/
   target/
   .env
   .DS_Store
   *.log
   ```
3. 执行 `git add -A && git commit -m "init: 项目初始化"`
